# 代码改进方案：对标"人气好店"竞品演示视频

> 依据：竞品演示视频功能清单 × 当前小魔推代码库(D:\xiaomotui)功能摸底
> 原则：保持现有"合规发布"设计(复制文案+唤起+截图审核)，不照抄竞品的"一键代发/自动好评"

## 一、功能差距总览

| 视频中的功能 | 当前状态 | 差距 |
|---|---|---|
| NFC 触碰 → 多功能聚合 H5 页 | ❌ 无聚合页，单设备单功能(trigger_mode 七选一) | **核心差距** |
| Wi-Fi 一键连接 | 🟡 后端完整，无顾客端页面 | 补前端即可 |
| 视频发布(抖音/快手/小红书/朋友圈/视频号) | 🟡 抖音/快手可用，朋友圈有插件无入口，视频号无 | 补入口+接入 |
| 发布时"换一批"文案 | ❌ 单条预置文案 | 改文案池 |
| AI 编辑封面 / AI 调色 | ❌ 无 | 新增，可复用 AI 服务 |
| 打卡点评(抖音/高德/百度/美团/大众点评) | ❌ 仅 SceneConfig 有 review_config 字段 | 全新顾客端模块 |
| 优惠团购商品列表+抢购 | 🟡 仅平台跳转，无商品列表 | 扩展 GroupBuyService |
| 节日活动大转盘抽奖 | ❌ 全无(仅红包活动 CRUD) | 全新模块 |
| 营销私域(加微信/加QQ) | 🟡 企微/微信已实现，无 QQ | 补 QQ |
| 数据统计 | ✅ 已实现(触发/跳转/联系动作) | 补新模块埋点 |

---

## 二、分模块改进方案

### 模块 1：顾客端 H5 聚合页（P0，视频核心交互）

**现状**：`api/app/service/NfcService.php` 按 `trigger_mode` 单功能分发(VIDEO/COUPON/WIFI/CONTACT/GROUP_BUY/PROMO)；`api/public/hub.html` 仅是测试页。

**改动**：
1. 新增 `trigger_mode = AGGREGATION`(聚合模式)，或扩展现有 `MENU` 模式为完整聚合页。
2. 后端新增聚合配置接口 `api/app/controller/Nfc.php::getAggregationPage()`：一次返回该设备/门店的全部区块配置(Wi-Fi、发布任务、团购商品、点评入口、私域、活动)，数据来源复用 `SceneConfig` + 各 Service。
3. 顾客端新建 H5 页面 `api/public/h5/aggregate.html`(Vue3 CDN 版或编译产物)：按"连Wi-Fi → 看活动 → 发视频/写点评 → 加私域"的顾客旅程排序区块，支持后台配置"今日主推"置顶。
4. admin 端扩展 `admin/src/views/activity/SceneConfigMatrix.vue`：增加聚合页区块排序/开关/主推配置。

**关键文件**：
- `api/app/controller/Nfc.php`、`api/app/service/NfcService.php`
- 新建 `api/app/service/AggregationPageService.php`
- 新建 `api/public/h5/aggregate.html`
- `admin/src/views/activity/SceneConfigMatrix.vue`

### 模块 2：Wi-Fi 顾客端页面（P0，后端已就绪）

**现状**：`api/app/service/WifiService.php` 已实现 iOS mobileconfig、Android WIFI: URI、二维码、频控、连接记录——只缺展示页。

**改动**：
1. 聚合页内嵌 Wi-Fi 区块：SSID 默认脱敏显示(如 `Li***`)，点击"一键连接"按 UA 分发 iOS/Android 连接方案，不再明文展示密码(对标视频中明文密码的安全短板，反超)。
2. 连接动作调用现有 `WifiService` 记录接口，进入设备统计分析。

### 模块 3：视频发布体验增强（P1）

**现状**：`api/app/action/plugins/` 已有抖音/快手/小红书/朋友圈等 8 个发布插件；`AbstractPublishAction.php` 拼装"#话题# @门店名"口令。顾客端仅开放抖音/快手。

**改动**：
1. **文案池 + "换一批"**：设备表 `promo_copywriting` 单字段改为文案池(新表 `xmt_copywriting_pool`，device_id 关联，或复用 AI 生成多条入库)；接口 `getPublishTask` 增加 `rotate=1` 参数随机返回下一条。
2. **朋友圈/视频号入口**：朋友圈分享插件已存在，在 uni-app `pages/promo/index.vue` 增加平台选择；视频号走微信开放平台分享链路(微信内 H5 分享卡片)。
3. **AI 封面/AI 调色**：`api/app/controller/AiContent.php` 扩展两个能力：视频截帧生成封面候选(3-4 张供选)、LUT 风格化滤镜；前端在发布预览页提供"AI 封面"按钮。

### 模块 4：打卡点评模块（P1，全新，合规版）

**现状**：仅 `api/app/model/SceneConfig.php` 有 `review_config/checkin_config` 字段和 admin 配置页，无顾客端页面、无文案生成。

**改动**：
1. 新建顾客端点评页 `api/public/h5/review.html`：展示平台入口(大众点评/美团/高德/百度/抖音点评)，每项配置平台跳转 URL(商家在 admin 录入门店各平台主页链接)。
2. **合规文案生成**(不照抄竞品"自动好评")：调用现有 AI 服务生成"评价灵感/草稿"(多条，声明"请根据真实体验修改后发布")，用户可复制后跳转平台手动发布。
3. admin 端 `SceneConfigMatrix.vue` 补各平台链接录入 + 评价灵感开关。
4. 埋点：新建 `xmt_review_actions` 表(platform, device_id, user_hash, action, created_at)，进 `Statistics.php` 统计。

### 模块 5：团购商品列表聚合（P1）

**现状**：`api/app/service/GroupBuyService.php` 仅生成平台跳转链接(美团/抖音/饿了么/自定义)，无商品列表。

**改动**：
1. 新表 `xmt_groupbuy_items`(merchant_id, platform, title, price, original_price, sales, image, jump_url, sort, status)。
2. admin 端新增团购商品管理页(录入商品截图/价格/链接)。
3. 聚合页团购区块：商品卡片列表(图+价+已售+抢购按钮)，按钮走现有 `group_buy_redirects` 跳转记录链路(UTM 埋点已具备)。

### 模块 6：大转盘抽奖活动（P2，全新模块）

**现状**：全库无 lottery/turntable 代码，仅有 `RedpacketActivity.php` 红包 CRUD。

**改动**：
1. **数据模型**：
   - `xmt_lottery_activities`(活动名、时间窗、门店/设备绑定、每日次数)
   - `xmt_lottery_prizes`(奖项、图标、概率、库存、类型:优惠券/谢谢参与/自定义)
   - `xmt_lottery_records`(user_hash、device_id、activity_id、奖品、状态)
2. **抽奖机会规则**(对标视频"您有一次抽奖机会"弹窗)：与 NFC 触发绑定——每次 `DeviceTrigger` 记录即产生 1 次抽奖机会(每日上限可配)，聚合页顶部弹窗提醒。
3. 顾客端 `api/public/h5/lottery.html` 转盘页(CSS 转盘动画即可，无需重型库)。
4. 中奖发券对接现有优惠券模块(`trigger_mode=COUPON` 体系)。
5. admin 端活动管理页：活动 CRUD、奖品配置、中奖记录导出。

### 模块 7：私域加粉补齐 QQ（P3）

**现状**：`api/app/service/ContactService.php` 已有企微链接/微信二维码/动作记录。

**改动**：联系配置增加 `qq_qrcode` 字段，顾客端加 QQ 入口，动作记录 type 增加 `QQ`。工作量极小。

---

## 三、实施路线图

| 阶段 | 内容 | 依赖 |
|---|---|---|
| **第一阶段** | 聚合页(模块1) + Wi-Fi 区块(模块2) | 无新表，复用现有 Service |
| **第二阶段** | 文案池换一批 + 团购商品列表(模块3部分、5) | 各 1 张新表 |
| **第三阶段** | 打卡点评合规版(模块4) | AI 服务现有 |
| **第四阶段** | 大转盘抽奖(模块6) + AI 封面(模块3) | 3 张新表 |
| **收尾** | 加QQ(模块7) | 半天 |

## 四、合规红线(不照抄竞品的部分)

1. **不做"一键代发"**：保持现有"复制口令+唤起 App+截图审核返奖励"模式，竞品的自动发布链路有封号风险。
2. **不做"自动好评"**：点评模块只做"评价灵感草稿+跳转平台"，文案明示"根据真实体验修改后发布"。
3. **Wi-Fi 密码不外显**：竞品明文展示密码(liyu123456)，我们做脱敏+一键连接，反而是卖点。

## 五、可复用的现有资产

- 触发埋点：`DeviceTrigger.php`(xmt_device_triggers)
- 跳转埋点：`group_buy_redirects` + UTM
- 联系埋点：`ContactService::recordContactAction`
- 统计出口：`Statistics.php`、`UserBehaviorAnalysisService.php`
- 发布插件体系：`api/app/action/plugins/` 8 个插件
- 场景配置：`SceneConfig` + `SceneConfigMatrix.vue`(点评模块的现成骨架)
