# 生产环境配置
# 重要：部署前必须修改以下配置为正式 HTTPS 域名
# 后端需在 CORS 白名单中允许该域名
# 方式一（推荐本地 preview）：留空，依赖 vite.config.js 中 preview.proxy 配置
# 方式二（用于正式部署）：替换为正式域名，例如 VITE_API_BASE_URL=https://api.example.com/api
VITE_API_BASE_URL=
